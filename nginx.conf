# nginx.conf is no longer used.
# The container serves static files using the 'serve' npm package instead,
# which natively reads the $PORT environment variable provided by Cloud Run.
# This file is kept for reference only and can be safely deleted.
